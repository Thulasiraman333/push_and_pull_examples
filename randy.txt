File created on the server but when you hit fetch button, it will just stored in your local repository but not in your file system
But when you hit pull it directly stored in your local repo as well as your local file system.
If you click fetch, then again merge into your local file system then only it will reflect in your local file system.
